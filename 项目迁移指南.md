# TraceEye 项目迁移指南

## 问题现状

你的项目缺少 `.gitignore` 文件，导致 `node_modules` 被 git 跟踪，无法正常提交。现在我已经为你创建了合适的 `.gitignore` 文件。

## ✅ 推荐的迁移方案（标准做法）

### 方案一：Git 仓库迁移（推荐）

**第1步：清理当前 git 状态**
```bash
# 移除已经被跟踪的 node_modules
git rm -r --cached trace-eye-frontend/node_modules
git rm -r --cached .idea/
git rm -r --cached .venv/

# 提交更改
git add .
git commit -m "Add .gitignore and remove ignored files from tracking"
```

**第2步：推送到远程仓库**
```bash
git remote add origin <你的新git仓库地址>
git push -u origin master
```

**第3步：在新电脑上克隆项目**
```bash
git clone <你的git仓库地址>
cd TraceEye

# 安装前端依赖
cd trace-eye-frontend
npm ci

# 安装Python依赖（如果有后端）
cd ..
pip install -r requirements.txt  # 如果有的话
```

**第4步：启动开发环境**
```bash
# 启动前端
cd trace-eye-frontend
npm run dev
```

### 方案二：压缩包迁移（如果没有Git仓库）

**第1步：创建项目压缩包**
```bash
# 在项目根目录，排除 node_modules 创建压缩包
tar --exclude='trace-eye-frontend/node_modules' \
    --exclude='.idea' \
    --exclude='.venv' \
    --exclude='.git' \
    -czf trace-eye-project.tar.gz .

# 或者使用 WinRAR/7-Zip 手动选择要打包的文件夹
# 不要包含：node_modules, .idea, .venv, .git
```

**第2步：传输到新电脑**
- 通过 U盘、网络传输等方式
- 解压到新电脑的目标目录

**第3步：在新电脑上安装依赖**
```bash
cd trace-eye-frontend
npm install

cd ..
pip install -r requirements.txt  # 如果有的话
```

### 方案三：网络问题时的解决方案

如果新电脑网络不好，无法下载依赖：

**第1步：打包 node_modules**
```bash
# 在原电脑上
cd trace-eye-frontend
tar -czf node_modules.tar.gz node_modules/
```

**第2步：传输并解压**
```bash
# 在新电脑上
cd trace-eye-frontend
tar -xzf node_modules.tar.gz
```

**注意：这种方式不如重新安装可靠，仅作为网络问题时的备选方案。**

## 🎯 重要说明

### 关于 package-lock.json
- ✅ **必须提交**：确保依赖版本完全一致
- ✅ **已在你的项目中**：`trace-eye-frontend/package-lock.json`
- ✅ **保证环境一致性**：`npm ci` 会使用这个文件安装精确版本

### 关于 .gitignore
我已经创建了两个 `.gitignore` 文件：

**项目根目录 `.gitignore`**：
```
.idea/          # IDE 配置
.venv/          # Python 虚拟环境
*.log           # 日志文件
```

**前端目录 `.gitignore`**：
```
node_modules/   # 依赖包
dist/           # 构建输出
.env*           # 环境变量
```

### 依赖安装命令对比

| 命令 | 用途 | 何时使用 |
|------|------|----------|
| `npm install` | 安装依赖，可能更新 package-lock.json | 开发时添加新包 |
| `npm ci` | 严格按照 package-lock.json 安装 | 部署、迁移、CI/CD |

## 🚀 快速迁移检查清单

- [ ] 清理 git 状态，移除 node_modules 跟踪
- [ ] 创建远程 git 仓库
- [ ] 推送代码到远程仓库
- [ ] 新电脑克隆项目
- [ ] 运行 `npm ci` 安装依赖
- [ ] 启动开发服务器测试

## 🔄 后续开发建议

1. **定期提交**：养成定期 `git add . && git commit` 的习惯
2. **忽略文件**：永远不要提交 `node_modules`、构建输出等
3. **环境一致性**：使用 `npm ci` 而不是 `npm install` 进行部署
4. **文档更新**：重要改动后及时更新 `claude-memory.md`

这样迁移后，你的项目就能在新电脑上正常运行了！